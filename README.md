# nevw

> starter pack electron+vuejs with DB support (nedb), webpack and buefy framework

## Build Setup

``` bash
# install dependencies
yarn

# serve with hot reload at localhost:8080 and start electron
in one terminal:	yarn dev
in another:			./node_modules/.bin/electron .

# build for production with minification


# build for production and view the bundle analyzer report

```

